"use strict"

let dest = ["Hawaii", "California", "New York", "Chicago"];
let rest = ["Chinese", "American", "Italian", "Japanese"];
let transpo = ["Car", "Train", "Plane", "Boat"]
let enter = ["Play", "Movie", "Sport Event", "Bar"]

let value = math.floor(math.random() * dest.length);
let rRest = math.floor(math.random() * rest.length);
let rTrans = math.floor(math.random() * transpo.length);
let rEnter = math.floor(math.random() * enter.length);
